# タスク

最終更新: 2026-07-12

---

## 実装済み

- `preprocess.py`: Otsu二値化・リサイズ・Adaptive Threshold fallback
- `ocr.py`: MSER + NMS + 横方向マージ + 切り抜きOCR (PSM 7) ＋ 段階別計装（`debug` 引数）
- `test_ocr.py`: `run_ocr_with_preprocess` 使用・debug画像4枚出力
- `tests/eval_ocr.py`: 評価ハーネス（2026-07-12）
- マスク生成の目視確認完了（2026-06-10）
- **段階1 完了（2026-07-12・[#1](https://github.com/TechTM-0/GraphicCopy/issues/1)）**
  - ベースライン固定（`tests/baseline.json`）: 検出再現率100% / 認識一致率66.7% / 誤検出1 / OCR呼び出し9回 / 空振り1 / 6.0秒
  - 「ー」消失 = **H4**（単独クロップでTesseractが空文字を返す）
  - 「タ」消失 = **H5**（`_deduplicate` が隣接文字を重複と誤認して除去）
  - 9文字すべてが検出フィルタを生存 → H1・H2 は棄却

---

## 未着手（優先順）

| 優先 | タスク | 内容 | 状態 |
|---|---|---|---|
| ★★★★★ | **段階2: `detect.py` 新規実装（A〜D）** | インク抽出・CC分析・2パス分類・行グルーピング（fable 5提案.md 節2・6）。完了条件: フロー.pngで「スタート」4文字を含む全文字のbbox取得。着手前に実装方針の説明と承認 | 未着手 |
| ★★★★☆ | 段階3: E〜G接続 | CCピクセル描画クロップ・行クロップ正規化・信頼度3帯域ゲート。`run_ocr_with_preprocess` の内部だけ差し替え | 未着手 |
| ★★★☆☆ | 段階4: ピクセルマスク化 | 採用CCのみのピクセル精度マスク・`empty_frame` 警告 | 未着手 |
| ★★★☆☆ | 評価サンプルの追加 | 紙の写真を最低1枚（二値化依存点の監視・fable 5提案.md 節5）。ドメイン別に3〜5枚 | 未着手 |
| ★★☆☆☆ | 段階5: MSER系コード削除 | 全サンプルで旧版以上の数値が出たときのみ実施 | 未着手 |
| ★★★★★ | 段階1: 評価ハーネス＋段階別計装＋ベースライン計測 | — | **完了（2026-07-12）** |
| ★★★★★ | fable 5提案のレビュー・採否判断 | — | **完了（2026-06-12）** |
| ★★★★★ | マスク画像の保存・目視確認 | — | **完了（2026-06-10）** |
| ★★★★☆ | 「タ」「ー」未検出の調査 | H4・H5 と判明。CC設計で解消される見込み | **完了（2026-07-12）** |
| ★☆☆☆☆ | ドキュメント自動更新フック | git commit の PostToolUse フックでドキュメント更新漏れを確認する仕組み | 保留（問題が起きる可能性あり） |

**優先順位の根拠**: 消失原因が確定し、CC設計が両方の機序を構造的に解消することが確認できた。旧パイプラインへの小手先の対策（縦長フィルタ・信頼度70・bboxパディング）は**すべて破棄**し、CC実装に直行する。

**破棄したタスク**: 縦長フィルタ追加・`CONFIDENCE_THRESHOLD` の変更・bboxパディング追加・単語結合 — いずれもMSERパイプライン前提の対症療法であり、CC設計（信頼度3帯域ゲート・行単位認識・ピクセルマスク）に包含されるため。

---

## Phase 別ロードマップ

### Phase 1: Pythonプロトタイプ（進行中）

- [x] 前処理モジュール（Otsu二値化・リサイズ）
- [x] MSER + NMS による小さいテキスト領域検出
- [x] テキストマスク生成（コードは存在するが未検証）
- [x] マスク生成の目視確認（2026-06-10完了）
- [ ] bboxパディング追加
- [ ] 縦長フィルタ追加
- [ ] 信頼度フィルタ調整（0 → 70）
- [ ] 基本図形抽出（直線・円）
- [ ] SVG出力

### Phase 2: 空間関係推定

- [ ] 距離ベースのテキスト→図形所属判定
- [ ] 包含・接触関係の検出
- [ ] 実サンプルで閾値チューニング

### Phase 3: 複雑図形対応

- [ ] ユーザーヒントによる形状制約の実装
- [ ] 楕円・多角形・自由曲線への対応
- [ ] 矢印・有向エッジの検出

### Phase 4: 展開

- [ ] Web版（WASM）
- [ ] モバイル版

